let name = "John";
let surname="Doe";

console.log(name); //prints
console.log(surname);

let age = 11;
console.log(age);
age = 54; //rewrites the age variable also variable is already declared so we dont need it  
console.log(age);

const pi = 3.4; //const cant overwrite the variable (reassign)
console.log(pi);